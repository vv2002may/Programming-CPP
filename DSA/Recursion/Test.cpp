#include "bits/stdc++.h"
using namespace std;
int main()
{
   set<int> s;
   s.insert(3);
   cout << s.begin();
   // int n, p;
   // cin >> n >> p;
   // vector<int> a(n);
   // for (int i = 0; i < n;i++)
   //    cin >> a[i];
   // vector<int> b(p);
   // for (int i = 0; i < p;i++)
   // {
   //    cin >> b[i];
   // }
   cout << endl;
}  